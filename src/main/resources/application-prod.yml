# "prod" suffix properties file. Scopes name samples: "prod", "web-prod", "api-prod", etc.
# Overrides application.yml default properties

spring:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQL8Dialect

  datasource:
    db: pcatteldb
    host: DB_MYSQL_DESAENV04_PCATTELDB_PCATTELDB_ENDPOINT
    username: pcatteldb_WPROD
    password: DB_MYSQL_DESAENV04_PCATTELDB_PCATTELDB_WPROD


jwt:
  secret: ${SECRET_TOKEN}
  expiration: 86400000